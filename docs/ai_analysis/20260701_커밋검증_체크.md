생성일: 2026-07-01
관련 프로젝트: church_finance (교회 재정 관리 앱)
작성자: Claude (AI)
신뢰도: 높음
분석 대상: 커밋 beecd4a, 5a47347, e1f9594 / src/lib/constants.ts / 입력 클라이언트 / /api/lookups

## 📌 TL;DR
- 붙여넣은 3개 커밋(①②③) 모두 실제 git 이력·코드와 정확히 일치. 개수도 검증 완료(지출 38·헌금 15·관 7).
- "반영 시점" 주의사항도 코드로 사실 확인: 입력 드롭다운은 하드코딩 상수(`constants.ts`), CRUD는 시트에 직접 씀 → 신규 항목은 재동기화 전까지 입력창에 안 뜸.
- 타입체크(`tsc --noEmit`) exit 0, 오류 0 / 워킹트리 clean → 이 계정에서 그대로 실행 가능.

## 검증 상세

### git 상태
- 브랜치 `main`, 워킹트리 clean (uncommitted 없음). 리포 루트는 `church_finance/app/`.
- 커밋 존재 확인:
  - `beecd4a` fix: 지출종류 드롭다운 지출분류 전체(38개) + 관 분류 — `constants.ts`, `types.ts`
  - `5a47347` feat: 코드값(룩업) CRUD — `api/lookups/route.ts`, `settings/codes/page.tsx`, `LookupManager.tsx`, `google-sheets.ts`, `AppShell.tsx`, `types.ts`
  - `e1f9594` feat: 재정집계표 + CSV — `finance/summary/page.tsx`, `AppShell.tsx`

### 개수 검증 (constants.ts)
| 분류 | 코드 개수 | 설명값 | 일치 |
|------|-----------|--------|------|
| 지출분류 EXPENSE_TYPES | 38 | 38 | ✓ |
| 헌금분류 OFFERING_TYPES | 15 | 15 | ✓ |
| 관 CATEGORIES(대분류) | 7 | — | ✓ |
| 소속 DEPARTMENTS | 6 | — | — |
| 직분 POSITIONS | 8 | — | — |

> 초기 자동 카운트가 39로 나온 건 `lookupName(list, key: string)` 함수 시그니처의 `key:`가 잡힌 오탐. 실제 배열 항목명은 사례비~기타 **38개**로 정확.

### "반영 시점" 주의사항 — 코드로 확정
- `expense/new/ExpenseInputClient.tsx:11` → `EXPENSE_TYPES.map(...)` (하드코딩 상수 사용)
- `offering/new/OfferingInputClient.tsx:27` → `OFFERING_TYPES.map(...)` (하드코딩 상수 사용)
- `/api/lookups` 소비처는 `LookupManager.tsx`(설정 CRUD 화면) **단 하나**. 입력·집계 화면은 이 API를 읽지 않음.
- 결론: 코드값 관리에서 신규 추가 → 시트엔 즉시 저장되나, 입력 드롭다운·목록엔 `constants.ts` 재동기화 전까지 안 나타남. **주의사항 그대로 정확.**

### 실행 가능성
- `npx tsc --noEmit` → exit 0, 오류 0 라인.
- `node_modules` 존재, `.next`·`.vercel` 존재.

## ⚠️ 가정 및 한계
- 시트 실제 데이터(38/15 실물)와 `constants.ts` 값의 1:1 대조는 시트 미열람으로 코드 기준만 확인. 커밋 메시지의 "총수입 29,001,000 일치"는 재실행 검증 안 함(런타임/시트 접근 필요).
- 소속(6)·직분(8)은 붙여넣은 설명에 개수 명시가 없어 참고치로만 기재.

## 📋 후속 작업
- [ ] (선택) 입력 드롭다운·집계를 시트/`/api/lookups`에서 실시간 로드하도록 전환 → 코드값 관리 추가분 즉시 반영 (중간 규모 작업)
- [ ] 실시간 연동 시 캐싱/로딩 상태·오프라인 폴백 설계 필요

## 🔗 관련 문서
- [20260630_재정집계표_구조분석.md](./20260630_재정집계표_구조분석.md)
- [20260629_UX_Feature_Redesign.md](./20260629_UX_Feature_Redesign.md)

## 📝 변경 이력
| 날짜 | 변경 내용 |
|------|----------|
| 2026-07-01 | 최초 작성 — 커밋 3개·개수·반영시점 주의사항·타입체크 검증 |
